# Direct dependencies — pinned to exact versions that pass all tests.
# To upgrade: change the version here, run pip install -r requirements.txt,
# run the full test suite, then regenerate requirements.lock with:
#   pip freeze > requirements.lock

# Core data + ML
pandas==2.3.3
numpy==2.4.6
scikit-learn==1.8.0

# AutoML (FLAML pulls LightGBM/XGBoost backends)
flaml[automl]==2.6.0
lightgbm==4.6.0
xgboost==2.1.4

# Experiment tracking + model registry
mlflow==3.12.0
joblib==1.5.3

# Deployment / serving
fastapi==0.136.3
uvicorn[standard]==0.48.0
streamlit==1.57.0

# Visualisation
matplotlib==3.10.9
plotly==5.24.1

# Monitoring
evidently==0.7.21
python-dotenv==1.2.2

# Carbon emissions tracking (optional; core pipeline works without)
codecarbon==3.2.7

# AutoML backends (H2O requires JVM 8+; optional — install separately)
h2o==3.46.0.11

# Testing
pytest==9.0.3
